package fr.misterassm.korden.models.enums

/* An enum class is a special kind of class that can only have a fixed number of instances */
enum class ClientAction {

    UNKNOWN, SEND, RECEIVE, SEND_AND_RECEIVE
}
